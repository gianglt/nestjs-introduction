const { MongoClient, ServerApiVersion } = require('mongodb');
const uri = "mongodb+srv://dnp:dnpadmin@dnp-db.znkgi.mongodb.net/?retryWrites=true&w=majority";
const client = new MongoClient(uri, { useNewUrlParser: true, useUnifiedTopology: true, serverApi: ServerApiVersion.v1 });
client.connect(err => {
  const collection = client.db("learning").collection("devices");
  // perform actions on the collection object
  client.close();
});



@Module({
  imports: [
    ProductsModule,
    MongooseModule.forRoot(
      'mongodb+srv://dnp:DphFUfGi1BQPHDOj@dnp-db.znkgi.mongodb.net/?retryWrites=true&w=majority'
    ),
  ],
  controllers: [AppController],
  providers: [AppService],
})